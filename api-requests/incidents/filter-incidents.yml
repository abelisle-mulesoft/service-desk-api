info:
  name: Filter incidents
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/api/incidents?status=OPEN&priority=HIGH"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
