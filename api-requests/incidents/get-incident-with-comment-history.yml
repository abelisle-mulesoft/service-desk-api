info:
  name: Get incident with comment history
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/api/incidents/{{commentHistoryIncidentId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false
