info:
  name: Empty update request
  type: http
  seq: 2

http:
  method: PATCH
  url: "{{baseUrl}}/api/incidents/{{openIncidentId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: "{}"
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: test("Status code is 400", function () { expect(res.getStatus()).to.equal(400); });

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
  forwardAuthorizationHeader: false

docs: "Expected: 400 Validation Failed."
